class Device:
    def __init__(self, DEVICE_NAME, INTERFACE, BAUD, SCRIPT, BUCKET, ORG, TOKEN, URL, PORT):
        self.DEVICE_NAME = DEVICE_NAME
        self.INTERFACE = INTERFACE
        self.BAUD = BAUD
        self.SCRIPT = SCRIPT
        self.BUCKET = BUCKET
        self.ORG = ORG
        self.TOKEN = TOKEN
        self.URL = URL
        self.PORT = PORT
    